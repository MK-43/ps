#include <fstream>
#include <iostream>
#include <vector>
using namespace std;

int N;

class Solution {
 public:
  vector<int> twoSum(vector<int>& nums, int target) {
    // sort first
    // nlog(n)

    nums.
  }
};

int main() {
#ifndef ONLINE_JUDGE
  freopen("input.txt", "r", stdin);
#endif
  cin >> N;

  vector<int> vec = {2, 7, 11, 15};

  Solution sol;
  sol.twoSum(vec, 9);

  return 0;
}